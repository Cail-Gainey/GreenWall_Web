{
    email {$ACME_EMAIL}
}

{$DOMAIN} {
    encode gzip zstd

    @backend path /api/* /callback*
    handle @backend {
        reverse_proxy {$BACKEND_UPSTREAM}
    }

    handle {
        reverse_proxy frontend:80
    }
}

www.{$DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}
